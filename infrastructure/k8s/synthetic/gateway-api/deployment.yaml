apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-api
  namespace: synthetic
  labels:
    app: gateway-api
    team: edge
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: gateway-api
  template:
    metadata:
      labels:
        app: gateway-api
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "5000"
        prometheus.io/path: "/metrics"
        loki.grafana.com/log-format: "json"
    spec:
      containers:
        - name: gateway-api
          image: demo-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 5000
              name: http
          env:
            - name: SERVICE_NAME
              value: "gateway-api"
            - name: SERVICE_PROFILE
              value: "gateway"
            - name: NAMESPACE
              value: "synthetic"
            - name: ENVIRONMENT
              value: "production"
            - name: PORT
              value: "5000"
            - name: AUTH_SERVICE_URL
              value: "http://auth-service.synthetic.svc.cluster.local:5000/health"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://otel-collector.observability.svc.cluster.local:4317"
            - name: PYTHONUNBUFFERED
              value: "1"
          resources:
            requests: {memory: "64Mi", cpu: "10m"}
            limits:   {memory: "512Mi", cpu: "300m"}
          readinessProbe:
            httpGet: {path: /health, port: 5000}
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: {path: /health, port: 5000}
            initialDelaySeconds: 10
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: gateway-api
  namespace: synthetic
spec:
  selector:
    app: gateway-api
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30540
  type: NodePort
